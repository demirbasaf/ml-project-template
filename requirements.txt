# Training + export. The browser demo needs nothing (plain JS on static JSON).
torch>=2.0
numpy>=1.24
# Only needed to re-derive data/names.txt from the raw Kaggle .xlsx (optional):
#   pandas>=2.0
#   openpyxl>=3.1
