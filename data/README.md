# data

Put your training data here (or a note on how to fetch it).

If your source data is large or not redistributable, **ship only the derived
form** your training actually consumes (e.g. a `names.txt`), and add the raw
source to the repo `.gitignore`. Document provenance + license in the root
README's "Data" section.
